const numeros = [100, 200, 300, 400, 500, 600];

// Primeira expressão: é executada apenas uma única vez criando uma variavel let com nome indice e valor 0.
// Segunda expressão: condição de execução, é executada a qunatidade de vezes até ser menor que o numero de variaveis dentro da const numeros.
// Terceira expressão: é executada sempre ao final do bloco que adiciona +1 no valor de indice até que satisfaça a segunda

for (let indice = 0; indice = numeros.length; indice++) {
    console.numeros([indice]);
}  